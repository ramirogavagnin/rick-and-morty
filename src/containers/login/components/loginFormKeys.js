const formKeys = {
    email: 'email',
    password: 'password',
};

const { email, password } = formKeys;

const initialValues = {
    [email]: '',
    [password]: '',
};

export { formKeys, initialValues };
